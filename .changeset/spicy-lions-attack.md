---
"agentic-debug-mode": minor
---

Automate releases with Changesets: merging a changeset opens a Version Packages PR that, once
merged, tags the release and dispatches the native build and npm/Homebrew publish pipeline.
